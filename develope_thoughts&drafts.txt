[Runtime] Starting...
[Metrics] fast: +788 (total=788, q=204) | slow: +198 (total=198, q=256)
[Metrics] fast: +796 (total=1584, q=407) | slow: +198 (total=396, q=256)
[Metrics] fast: +793 (total=2377, q=610) | slow: +198 (total=594, q=256)
[Metrics] fast: +795 (total=3172, q=807) | slow: +200 (total=794, q=204)
[Metrics] fast: +793 (total=3965, q=1013) | slow: +198 (total=992, q=6)
[Metrics] fast: +735 (total=4700, q=292) | slow: +8 (total=1000, q=0)
[Metrics] fast: +300 (total=5000, q=0) | slow: +0 (total=1000, q=0)
[Metrics] fast: +0 (total=5000, q=0) | slow: +0 (total=1000, q=0)
[Metrics] fast: +0 (total=5000, q=0) | slow: +0 (total=1000, q=0)
[Metrics] fast: +0 (total=5000, q=0) | slow: +0 (total=1000, q=0)

我们重点关注一下q，q返回的是fixedpool的task数量，这个task是在runtime的start函数里边submit的，
在main函数中定义了handler并绑定到了runtime rt上，然后去执行的对吧。
q返回的len(p.tasks)，task是个管道，那也就是说这里返回的最大值，也就是我们定义的fixedpool中的queue了
（原因是这一行：tasks: make(chan func(), max(0, queue)),）
所以我们会观察到有趣的现象，左边的q最大久1k多点了，超不多main函数定义的2048，
但是右边的q触碰到了任务管道容量的阈值256，也就导致，他连着三个周期的q都是满的，即256，
也就是说，在这期间，其实有很多数据是在生产端积压着无法被消费甚至都没办法入等待队列
所以在我后期实现负载均衡的时候，这就是一个典型的场景需要我去解决

We’ll focus on q. q returns the number of tasks in the fixedPool, and these tasks are submitted in the start function of runtime.
In main, a handler is defined and bound to the runtime rt, and then executed

q returns len(p.tasks), and tasks is a channel. That means the maximum value returned here is exactly the queue size we configured in fixedPool
(because of this line: tasks: make(chan func(), max(0, queue)),)

So we observe an interesting phenomenon: on the left side, the max q is a bit over 1k, almost the 2048 defined in main,
but on the right side, q hits the task channel capacity limit of 256. As a result, q stays full (256) for three consecutive intervals.

That means during this period, a lot of data is actually being backlogged on the producer side — it can’t be consumed, and can’t even enter the waiting queue.

So when I implement load balancing later, this is exactly the kind of scenario I’ll need to address.



Thoughts about Queue：（2025/12/02）
// queue只是允许等待消费的数据条数，不产生任何对实际消费速率的影响，可以理解为只是一个自我高潮的指标
// 肌肉大不大还是得看size，和我们消费的handler
// 那queue有啥必要呢？ 自我高潮对于监控来说非常有意义，
// 对于敏感的服务，我们设小一点，这样可以更快的被负载出去
// 对于不敏感的服务，我们可以设大一点；
// 自始至终这就是一个监控指标，多堵算堵，多快算快，还是用户说了算

// `queue` only sets how many tasks can wait for consumption; it doesn't affect actual throughput.
// Think of it as a "feel-good" metric for observability.
// Real muscle (processing power) depends on `size` and the handler's performance.
// So why bother with `queue`? Because this "feel-good" metric is crucial for monitoring.
// For latency-sensitive services, keep it small so backpressure kicks in faster.
// For less sensitive services, make it larger to buffer bursts.
// At the end of the day, it's a monitoring/tuning knob: "how full is too full" and "how fast is fast enough"
// are entirely up to the user's requirements.


// 用户需要做的事情：

1. 注册strategy
// 用户可以使用内置策略，也可以自己写
registry := scheduler.NewStrategyRegistry()
registry.Register("pressure_rebalance", &PressureRebalanceStrategy{})
registry.Register("my_custom", &MyCustomStrategy{})  // 自定义

2. 配置binding
// 告诉调度器："stream_a 绑定了这两个池"
bindings := scheduler.NewBindingRegistry()
bindings.Set("stream_a", 
    []pool.WorkerPool{poolFast, poolSlow}, 
    []int{8, 2})

3. 配置Policy
// 为每个流配置策略
policies := scheduler.StaticPolicyProvider{
    Default: Policy{
        Enabled: true,
        StrategyName: "pressure_rebalance",
    },
    Per: map[string]Policy{
        "stream_a": {StrategyName: "my_custom"},  // 用自定义策略
        "stream_b": {Enabled: false},             // 禁用自动调度
    },
}

4. 启动controller
controller := scheduler.NewController(
    provider, registry, policies, applier, bindings, sink, opts)
go controller.Run(ctx)

T0 (10:00:00) - Controller 启动
    ↓
T2 (10:00:02) - 第一次 tick
    ├─ Sample: 获取所有池的指标
    ├─ 遍历 stream_a:
    │   ├─ Policy: {Enabled:true, Strategy:"time_based", Cooldown:5s}
    │   ├─ Strategy: TimeBasedStrategy.DecideStream()
    │   │   └─ 当前 10:00，是工作时间
    │   │   └─ 生成 Plan: [9, 1]
    │   ├─ 冷却检查: 第一次调整，不在冷却期
    │   ├─ Apply: router.SetMany("stream_a", [poolFast, poolSlow], [9,1])
    │   └─ 标记: lastApply["stream_a"] = 10:00:02
    ├─ 遍历 stream_b:
    │   └─ Policy: {Enabled:false}
    │   └─ 跳过（禁用）
    
T4 (10:00:04) - 第二次 tick
    ├─ 遍历 stream_a:
    │   ├─ Strategy 想调整 [9,1] → [8,2]
    │   ├─ 冷却检查: 10:00:04 - 10:00:02 = 2秒 < 5秒
    │   └─ 拦截，跳过
    
T9 (10:00:09) - 第四次 tick
    ├─ 遍历 stream_a:
    │   ├─ 冷却检查: 10:00:09 - 10:00:02 = 7秒 > 5秒
    │   ├─ Apply: router.SetMany("stream_a", [...], [8,2])
    │   └─ 标记: lastApply["stream_a"] = 10:00:09


Controller.Run()
    ↓
Provider.Sample() → 采集指标
    ↓
Policy.Get() → 查询该流的配置
    ↓
Registry.Get() → 查找策略
    ↓
Strategy.DecideStream() → 生成 Plan
    ↓
inCooldown() → 检查冷却
    ↓
Applier.Apply() → 应用到 Router
    ↓
markApplied() → 记录时间
